{
	"private": true,
	"packageManager": "pnpm@6.32.3",
	"description": "Celebrate UI Monorepo.",
	"license": "MIT",
	"engines": {
		"node": ">= 16"
	},
	"scripts": {
		"dev": "pnpm -C playground dev",
		"typecheck": "pnpm vue-tsc --noEmit -p tsconfig.web.json --composite false && pnpm vue-tsc --noEmit -p tsconfig.playground.json --composite false && pnpm tsc --noEmit -p tsconfig.node.json && pnpm tsc --noEmit -p tsconfig.vite-config.json && pnpm vue-tsc --noEmit -p tsconfig.vitest.json --composite false"
	},
	"devDependencies": {
		"@celebrate-ui/eslint-config": "workspace:^1.0.0",
		"@types/node": "*"
	}
}
